import {
  formatPhone,
  formatPhoneKey,
} from "../../../utils/formatPhoneNumber.mjs";
import { replyChatWA } from "../sender.mjs";

export async function handleDaftarPemeriksaan(msg) {
  const from = msg.from;
  const text = msg.body.trim();
}
